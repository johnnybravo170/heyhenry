/**
 * Re-exports every Drizzle table in one place. Pass this module to
 * `drizzle(...)` so the type-safe client knows about all tables at once.
 */

export * from './tenant-members';
export * from './tenants';
